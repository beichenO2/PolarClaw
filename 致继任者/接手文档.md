# MyClaw — 接手文档

> 最后更新：2026-04-15

## 环境定位

- **工作空间**：`~/Polarisor/`
- **本项目路径**：`~/Polarisor/MyClaw/`
- **GitHub 仓库**：待创建

## 项目概述

MyClaw 是 AI Agent 融合平台，采用端口-适配器（六边形）架构。从旧版 OpenClaw+DeerFlow+Hermes 融合项目完全重写为 TypeScript。

### 架构决策

- **端口-适配器**：所有外部依赖通过接口隔离，任何模块可独立替换
- **TypeScript**：从旧版 JavaScript 迁移，获得类型安全
- **多轮对话**：修复旧版最大缺陷（单轮无状态），SQLite 持久化对话历史
- **隐私优先**：Channel Privacy Gateway 在所有消息路径上强制脱敏

## 技术栈

| 组件 | 技术 |
|------|------|
| 语言 | TypeScript (ES2022) |
| 运行时 | Node.js 20+ |
| 数据库 | SQLite + FTS5 (better-sqlite3) |
| LLM | 阿里云百炼 Coding Plan (OpenAI-compatible) |
| 隐私 | PolarPrivate API (端口 8790) |

## 快速启动

```bash
cd ~/Polarisor/MyClaw
# 确保 PolarPrivate 在 8790 运行且 Vault 已解锁
npm run dev    # 开发模式（tsx watch）
# 或
npm run build && npm start  # 生产模式
```

默认开启 CLI 通道（`MYCLAW_CLI=1`），终端直接输入即可对话。
LLM 请求通过 PolarPrivate Proxy 转发（`http://127.0.0.1:8790/proxy/llm.aliyun.codingplan`），无需直接持有 API Key。

## 关键文件

| 文件 | 说明 |
|------|------|
| `src/ports/` | 7 个接口定义（修改接口需同步更新适配器） |
| `src/core/agent.ts` | Agent 核心循环（ReAct 多轮 + 并发工具调用） |
| `src/adapters/channel/cli.ts` | CLI 通道（终端交互） |
| `src/adapters/channel/feishu.ts` | 飞书通道（WebSocket + Webhook） |
| `src/adapters/llm/openai-compatible.ts` | LLM 路由器（意图检测 + Fallback + 熔断器） |
| `src/adapters/privacy/` | 隐私网关（PII 检测 + PolarPrivate 集成） |
| `src/adapters/compression/summarizer.ts` | 三阶段上下文压缩 |
| `skills/clock-integration/` | Clock 番茄钟集成技能（6 个工具） |

## 旧代码位置

旧 MyClaw 的所有代码已移到 `~/Desktop/ClawBin/2026-04-14/myclaw-cleanup/`，可作为参考。

## 注意事项

- **PolarPrivate 必须在线**：LLM 调用和隐私网关都依赖 PolarPrivate（端口 8790），Vault 需解锁
- Clock 集成需要 PolarClock 后端运行在端口 15550
- 飞书通道需要配置 App 凭证（`FEISHU_ADMIN_APP_ID` 等）
- `apiKey=proxy-managed` 是特殊标记，告知 LLM 适配器不发送 Authorization header（交由 Proxy 注入）
