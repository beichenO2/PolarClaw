# 进度

> 最后更新：2026-04-15 (飞书端到端联调后)

## 已完成

- [x] 清理旧项目（submodule、旧 apps/ → ClawBin）
- [x] TypeScript 项目骨架 + 端口-适配器架构
- [x] 7 个核心端口接口（channel, privacy, memory, llm, tools, skills, compression）
- [x] SQLite + FTS5 记忆存储适配器
- [x] 持久化对话历史（SQLite，进程重启恢复上下文）
- [x] LLM 路由器（意图检测 + Provider Fallback + 半开熔断器）
- [x] 工具执行器
- [x] 隐私网关（PII 正则 + PolarPrivate 集成 + Secret 拦截）
- [x] Agent 核心循环（多轮 ReAct、并发工具调用、记忆注入、上下文压缩）
- [x] Clock Integration Skill（6 个工具：user_context, tasks, timer_status, schedule, create_task, complete_task）
- [x] Skill 加载器（扫描 SKILL.md + 动态 import tools.ts）
- [x] 上下文压缩器（三阶段渐进：结构裁剪 → 头尾保护+折叠 → LLM 摘要）
- [x] 飞书通道适配器（WebSocket + Webhook 双模式，多 Bot 支持）
- [x] CLI 通道适配器（终端交互式对话，本地开发测试用）
- [x] .env 配置修复（PolarPrivate 端口 8787→8790，key 名对齐 config.ts）
- [x] PolarPrivate Proxy 集成（LLM 请求通过 Proxy 转发，不再直接持有 API Key）
- [x] TypeScript 编译通过，0 错误
- [x] **端到端验证通过**：CLI 对话 → PolarPrivate Proxy → 阿里云百炼 → 正常回复
- [x] **自学习技能系统**（4 个 Phase 全部完成）：
  - [x] Phase 1: 热加载 — SkillRegistry + 文件监听 + 运行时增删技能
  - [x] Phase 2: 反馈学习 — 工具使用追踪（TrackedToolExecutor）+ SQLite 反馈存储 + 偏好查询
  - [x] Phase 3: 技能生成 — 对话模式识别（PatternDetector）+ 自动生成 SKILL.md + tools.ts（SkillGenerator）
  - [x] Phase 4: 技能组合 — 工作流引擎（SkillComposer：串行/并行/条件分支 + 步骤间数据映射）
  - [x] 6 个 Agent 工具注册（learning_record_feedback, learning_get_preferences, learning_detect_patterns, learning_generate_skill, learning_list_skills, learning_compose_workflow）
  - [x] SOUL.md 更新（Agent 知道如何使用学习能力）

- [x] 设计文档 vs 实现核对 + 修复（README 目录结构更新、端口号默认值对齐、死代码清理）

## 已清理
- conversation-history.ts（内存版对话历史）→ `~/Desktop/ClawBin/2026-04-15/dead-code/`，被 persistent-conversation.ts 取代

### 架构审计修复 + Secrets 自动加载 (2026-04-15)
- [x] 默认 LLM 配置改为 PolarPrivate Proxy 模式（`proxy-managed` + Proxy URL）
  - 之前默认直连 `https://coding.dashscope.aliyuncs.com`，需手动设 env 才走 Proxy
  - 现在默认走 `http://127.0.0.1:12790/proxy/llm.aliyun.codingplan/v1`，无 API Key 时自动使用
  - 仍支持 `MYCLAW_LLM_BASE_URL` / `MYCLAW_LLM_API_KEY` 环境变量覆盖
- [x] PolarPrivate Secrets 自动加载器 (`secrets-loader.ts`)
  - 启动时自动从 Vault 读取 MyClaw 项目的 Secrets，注入 process.env
  - dot-notation → UPPER_SNAKE_CASE 映射 (如 `feishu.admin.app_id` → `FEISHU_ADMIN_APP_ID`)
  - 手动 .env 优先级高于 Vault，PolarPrivate 不可达时静默降级
  - 启动流程：`loadEnvFileEarly()` → `loadSecretsToEnv()` → `loadConfig()`
- [x] `tsconfig.tsbuildinfo` 加入 `.gitignore`

### 飞书端到端联调 (2026-04-15)
- [x] .env 变量名修复：`FEISHU_*` → `FEISHU_ADMIN_*`（代码 `loadFeishuConfig('FEISHU_ADMIN')` 需要带前缀）
- [x] 启用飞书通道：`MYCLAW_FEISHU=1`
- [x] **飞书 WebSocket 连接验证通过**：管理员 Bot + 女友 Bot 双通道上线
- [x] **飞书消息收发验证通过**：用户发消息 → Agent 处理 → 飞书回复
- [x] 修复重复自我介绍：对话进行中注入 `isOngoing` 信号，告知 LLM 不要重复介绍
- [x] 消息串行化：同一用户消息通过 Promise 链锁顺序处理，避免对话历史竞争
- [x] Skill 加载器生产模式修复：通过 `tsx/esm` register 支持 .ts skill 动态导入，优先查找 .js

### 注意事项（给继任者）
- PolarPrivate 必须从 `~/Polarisor/PolarPrivate/backend/` 目录启动，否则 `sqlite:///./privportal.db` 会在错误位置创建空数据库
- PolarPrivate 偶尔会 hang（TCP 连接成功但不响应），需要 `kill` + 重启
- 重启 PolarPrivate 后需要在 http://localhost:5173 重新解锁 Vault

### AutoOffice + KnowLever 集成接口 (2026-04-15)
- [x] autooffice-integration Skill 创建（6 个工具：health, generate_report, summarize, enrich, check_quality, list_templates）
- [x] knowlever-integration Skill 创建（3 个工具：query, list_topics, ingest）
- [x] TypeScript 编译通过
- [x] 致继任者文档更新（相关项目 + 集成接口详情 + 对端阻塞项）
- [ ] **等待 AutoOffice**: `npm install` 恢复 node_modules → API server 可启动 → 端到端验证
- [ ] **等待 KnowLever**: 知识库灌入真实数据 → RAG 检索端到端验证
- [ ] **等待 SOTAgent**: AutoOffice 注册到项目字典

### 主动关怀引擎 + YOLO 自主执行引擎 ✅ (2026-04-15 确认)

此前标为"待做/迁移"，但实际已完整实现：
- [x] **主动关怀引擎** `src/adapters/proactive/care-engine.ts` (208 行)
  - IProactiveEngine 接口实现（start/stop/trigger/addRule/removeRule/listRules）
  - CarePolicy 三种策略：不活跃检测、番茄钟完成、定时关怀
  - 安静时段保护（8:00-22:00 外不打扰）
  - 同一用户两次关怀最小间隔控制
  - Clock 工具集成（timer_status）
  - main.ts 已集成：`MYCLAW_PROACTIVE=1` 环境变量启用
- [x] **YOLO 自主执行引擎** `src/adapters/yolo/engine.ts` (244 行)
  - IYoloEngine 接口实现（run/cancel/getSession）
  - 多步自主执行循环（通过反复调用 Agent.handleMessage）
  - 四重预算控制：maxSteps / maxTotalTokens / maxWallTimeMs / maxRetries
  - 目标达成检测（信号词匹配）
  - 用户介入升级（"需要用户确认"检测）
  - main.ts 已注册为 `yolo_start` 工具
- [x] **错误恢复策略** `src/adapters/yolo/recovery.ts` (72 行)
  - 瞬态错误自动重试（网络/超时/429/503）
  - 不可恢复错误直接中止（billing/unauthorized/403）
  - 工具不存在自动跳过
  - 指数退避重试
- [x] **Agent 工具注册**：`yolo_start` + `care_add_rule`
- [x] TypeScript 编译通过，0 错误

注意：旧代码 `apps/proactive/care.mjs` 和 `apps/yolo/engine.mjs` 不存在于 ClawBin 中，
当前实现是在端口-适配器架构中重新设计的全新版本。

### 测试覆盖补全 (2026-04-15)
- [x] vitest.config.ts 配置完成
- [x] 8 个测试文件，77 个测试用例，全部通过
  - agent 核心循环（消息流、隐私拦截、工具调用、token 累计、轮数上限）
  - 工具执行器（注册/卸载/超时/钩子）
  - PII 检测器（手机/邮箱/身份证/IP/自定义实体/还原）
  - SQLite 记忆存储（FTS5 搜索/用户画像 CRUD）
  - 持久化对话历史（追加/隔离/截断/跨实例持久化）
  - 三阶段上下文压缩器（Phase 1/2 验证 + Phase 3 限制记录）
  - 主动关怀引擎（策略评估 + 规则管理 + 频率限制）
  - YOLO 引擎（目标达成/步数限制/token 预算/升级/恢复策略）

## 待做
- [ ] IterationBudget 预算控制（Agent 核心循环级别，与 YOLO 预算互补）
- [ ] MCP 协议集成
