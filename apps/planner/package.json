{
  "name": "@myclaw/planner",
  "version": "0.0.1",
  "type": "module",
  "description": "Flexible planning engine for MyClaw — deviation-tolerant plans, task linking, experience-driven prediction",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  }
}
